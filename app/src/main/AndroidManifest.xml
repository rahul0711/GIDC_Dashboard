<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyGIDC">

        <!-- Splash is FIRST screen -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />

        <!-- Dashboard screens -->
        <activity android:name=".dashboard.AdminDashboardActivity" />
        <activity android:name=".dashboard.EngineerDashboardActivity" />
        <activity android:name=".dashboard.AgencyDashboardActivity" />
        <activity android:name=".dashboard.HeadDashboardActivity" />
        <activity android:name=".dashboard.DepartmentStatsActivity" />
        <activity android:name=".dashboard.ComplaintListActivity" />
        <activity android:name=".dashboard.ComplaintDetailActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Settings / Logout ← ADD THIS -->
        <activity
            android:name=".settings.LogoutActivity"
            android:exported="false" />

    </application>

</manifest>